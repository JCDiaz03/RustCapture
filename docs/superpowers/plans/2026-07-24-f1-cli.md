# F1 — CLI fina sobre el core (f.8, D1) — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** El primer binario usable del proyecto: `rustcapture.exe [--fullscreen|--window|--region X,Y,WxH] [--clipboard|--file [--dir RUTA] [--format png|jpg]]` capturando de punta a punta (GDI → modo → sink real) a través del bus de eventos.

**Architecture:** Binario fino (D1): `args.rs` traduce flags a `ModeRequest` + destino (parsing puro, TDD) y `main.rs` solo cablea — DPI awareness, `Orchestrator` con `GdiScreenSource` + `create_mode` + el sink elegido, y un `CaptureRequested` por el canal mpsc (D7: la CLI es un productor más). Sin lógica de dominio en el binario. El binario se llama `rustcapture` (el paquete sigue siendo `cli`).

**Tech Stack:** `pico-args` 0.5 (parser de flags sin dependencias, fiel a la prioridad de peso mínimo; nada de `clap`).

## Global Constraints

- Binario fino (D1): cero lógica de dominio en `cli`; todo pasa por el orquestador y el bus (D7).
- Prioridad peso mínimo: `pico-args` (cero deps transitivas), no `clap`.
- Defaults amables para scripting (f.8): sin flags = `--fullscreen --clipboard`.
- TDD en el parsing (`args.rs`); el cableado de `main.rs` se verifica ejecutando el binario real (esta máquina es Windows).
- Comentarios y rustdoc en español. `cargo fmt` antes de cada verificación.
- **Commits: SOLO con aprobación humana previa** (skills.md). Único commit al final: `v0.1.6 — F1: CLI fina (rustcapture.exe)`.
- Códigos de salida: 0 éxito, 1 fallo de captura/entrega, 2 argumentos inválidos (imprime uso en stderr).
- `unwrap`/`expect` permitidos en `main.rs` solo para invariantes locales (canal recién creado, sink único); nunca sobre resultados de captura.

---

### Task 1: Parsing de argumentos (`cli/src/args.rs`)

**Files:**
- Modify: `Cargo.toml` (workspace: añadir `pico-args = "0.5"`)
- Modify: `crates/cli/Cargo.toml` (añadir `pico-args`, `[[bin]] name = "rustcapture"`)
- Create: `crates/cli/src/args.rs`
- Modify: `crates/cli/src/main.rs` (añadir `mod args;` para que compile con los tests)

**Interfaces:**
- Consumes: `rustcapture_core::capture::ModeRequest`, `rustcapture_core::output::ImageFormat`, `rustcapture_core::ports::Rect`.
- Produces: `CliOptions { mode: ModeRequest, destination: Destination }`; `Destination { Clipboard, File { dir: PathBuf, format: ImageFormat } }` (ambos `Debug + PartialEq`); `parse(raw: Vec<OsString>) -> Result<CliOptions, String>`; `USAGE: &str`. Task 2 los consume desde `main`.

- [ ] **Step 1: Declarar dependencia y bin**

En el `Cargo.toml` del workspace, bajo `[workspace.dependencies]`:

```toml
pico-args = "0.5"
```

En `crates/cli/Cargo.toml`, añadir tras `[package]`:

```toml
[[bin]]
name = "rustcapture"
path = "src/main.rs"
```

y bajo `[dependencies]`:

```toml
pico-args = { workspace = true }
```

- [ ] **Step 2: Escribir los tests que fallan**

Crear `crates/cli/src/args.rs`:

```rust
//! Parsing de flags (f.8): puro y testeable; los errores devuelven un
//! mensaje listo para stderr.

use std::ffi::OsString;
use std::path::PathBuf;

use rustcapture_core::capture::ModeRequest;
use rustcapture_core::output::ImageFormat;
use rustcapture_core::ports::Rect;

#[cfg(test)]
mod tests {
    use super::*;

    fn p(args: &[&str]) -> Result<CliOptions, String> {
        parse(args.iter().map(OsString::from).collect())
    }

    #[test]
    fn sin_flags_es_fullscreen_a_portapapeles() {
        let opts = p(&[]).unwrap();
        assert_eq!(opts.mode, ModeRequest::Fullscreen);
        assert_eq!(opts.destination, Destination::Clipboard);
    }

    #[test]
    fn window_y_region_mapean_a_sus_modos() {
        assert_eq!(p(&["--window"]).unwrap().mode, ModeRequest::ActiveWindow);
        assert_eq!(
            p(&["--region", "10,-20,300x200"]).unwrap().mode,
            ModeRequest::Region(Rect::new(10, -20, 300, 200))
        );
    }

    #[test]
    fn file_con_dir_y_format() {
        let opts = p(&["--file", "--dir", "C:/caps", "--format", "jpg"]).unwrap();
        assert_eq!(
            opts.destination,
            Destination::File {
                dir: PathBuf::from("C:/caps"),
                format: ImageFormat::Jpeg
            }
        );
    }

    #[test]
    fn file_sin_dir_usa_el_directorio_actual_y_png() {
        let opts = p(&["--file"]).unwrap();
        assert_eq!(
            opts.destination,
            Destination::File {
                dir: PathBuf::from("."),
                format: ImageFormat::Png
            }
        );
    }

    #[test]
    fn dos_modos_a_la_vez_es_error() {
        assert!(p(&["--fullscreen", "--window"]).is_err());
    }

    #[test]
    fn region_mal_formada_es_error() {
        assert!(p(&["--region", "10,20"]).is_err());
        assert!(p(&["--region", "a,b,cxd"]).is_err());
    }

    #[test]
    fn format_sin_file_es_error() {
        assert!(p(&["--format", "png"]).is_err());
    }

    #[test]
    fn flag_desconocida_es_error() {
        assert!(p(&["--sepia"]).is_err());
    }
}
```

En `crates/cli/src/main.rs`, sustituir el contenido por:

```rust
//! Binario fino de línea de comandos (D1, f.8): traduce flags a llamadas
//! del core, sin lógica propia.

mod args;

fn main() {}
```

- [ ] **Step 3: Verificar que falla**

Run: `cargo test -p cli`
Expected: FAIL — `cannot find fn parse` / `CliOptions`.

- [ ] **Step 4: Implementar el parsing**

En `args.rs`, entre los `use` y los tests:

```rust
pub const USAGE: &str = "\
rustcapture — captura de pantalla (MVP F1)

USO:
  rustcapture [MODO] [DESTINO]

MODOS (por defecto: --fullscreen):
  --fullscreen            escritorio virtual completo
  --window                ventana activa
  --region X,Y,WxH        región en coordenadas de escritorio (p. ej. 0,0,800x600)

DESTINO (por defecto: --clipboard):
  --clipboard             copiar al portapapeles
  --file                  guardar en archivo con nombre automático
  --dir RUTA              directorio de destino (con --file; por defecto: .)
  --format png|jpg        formato de archivo (con --file; por defecto: png)
";

#[derive(Debug, PartialEq)]
pub struct CliOptions {
    pub mode: ModeRequest,
    pub destination: Destination,
}

#[derive(Debug, PartialEq)]
pub enum Destination {
    Clipboard,
    File { dir: PathBuf, format: ImageFormat },
}

pub fn parse(raw: Vec<OsString>) -> Result<CliOptions, String> {
    let mut args = pico_args::Arguments::from_vec(raw);

    let fullscreen = args.contains("--fullscreen");
    let window = args.contains("--window");
    let region: Option<String> = args
        .opt_value_from_str("--region")
        .map_err(|e| e.to_string())?;
    let mode = match (fullscreen, window, &region) {
        (_, false, None) => ModeRequest::Fullscreen,
        (false, true, None) => ModeRequest::ActiveWindow,
        (false, false, Some(spec)) => ModeRequest::Region(parse_region(spec)?),
        _ => return Err("elige un único modo: --fullscreen, --window o --region".into()),
    };

    let clipboard = args.contains("--clipboard");
    let file = args.contains("--file");
    let dir: Option<PathBuf> = args
        .opt_value_from_str("--dir")
        .map_err(|e| e.to_string())?;
    let format_raw: Option<String> = args
        .opt_value_from_str("--format")
        .map_err(|e| e.to_string())?;

    let destination = match (clipboard, file) {
        (true, true) => return Err("elige un único destino: --clipboard o --file".into()),
        (_, false) if dir.is_some() || format_raw.is_some() => {
            return Err("--dir y --format solo aplican con --file".into());
        }
        (_, false) => Destination::Clipboard,
        (false, true) => {
            let format = match format_raw.as_deref() {
                None | Some("png") => ImageFormat::Png,
                Some("jpg") | Some("jpeg") => ImageFormat::Jpeg,
                Some(otro) => return Err(format!("formato desconocido: {otro} (png|jpg)")),
            };
            Destination::File {
                dir: dir.unwrap_or_else(|| PathBuf::from(".")),
                format,
            }
        }
    };

    let sobrantes = args.finish();
    if !sobrantes.is_empty() {
        return Err(format!("argumentos no reconocidos: {sobrantes:?}"));
    }
    Ok(CliOptions { mode, destination })
}

/// "X,Y,WxH" → `Rect` (X e Y admiten negativos, multi-monitor).
fn parse_region(spec: &str) -> Result<Rect, String> {
    let err = || format!("región inválida: {spec} (esperado X,Y,WxH)");
    let mut partes = spec.split(',');
    let x: i32 = partes
        .next()
        .and_then(|s| s.trim().parse().ok())
        .ok_or_else(err)?;
    let y: i32 = partes
        .next()
        .and_then(|s| s.trim().parse().ok())
        .ok_or_else(err)?;
    let tam = partes.next().ok_or_else(err)?;
    if partes.next().is_some() {
        return Err(err());
    }
    let (w, h) = tam.trim().split_once('x').ok_or_else(err)?;
    let width: u32 = w.parse().map_err(|_| err())?;
    let height: u32 = h.parse().map_err(|_| err())?;
    Ok(Rect::new(x, y, width, height))
}
```

Nota del brazo `(_, false, None)`: `--fullscreen` explícito y el default comparten brazo; la exclusión mutua real está en los brazos de error.

- [ ] **Step 5: Verificar que pasa**

Run: `cargo fmt && cargo test -p cli`
Expected: PASS (8 tests).

- [ ] **Step 6: Staging**

```bash
git add Cargo.toml Cargo.lock crates/cli/
```

---

### Task 2: Cableado en `main.rs` y verificación con el binario real

**Files:**
- Modify: `crates/cli/src/main.rs`

**Interfaces:**
- Consumes: `args::{parse, CliOptions, Destination, USAGE}` (Task 1); `platform_win::{dpi, gdi::GdiScreenSource, clipboard::ClipboardSink}`; `rustcapture_core::{capture::create_mode, orchestrator::{AppEvent, CaptureRequest, Orchestrator}, output::FileSink}`.
- Produces: binario `rustcapture.exe` funcional con códigos de salida 0/1/2.

- [ ] **Step 1: Implementar el cableado**

Sustituir `crates/cli/src/main.rs` por:

```rust
//! Binario fino de línea de comandos (D1, f.8): traduce flags a un evento
//! del bus (D7) y deja que el orquestador del core haga el resto.

mod args;

use std::process::ExitCode;
use std::sync::mpsc;

use platform_win::clipboard::ClipboardSink;
use platform_win::gdi::GdiScreenSource;
use rustcapture_core::capture::create_mode;
use rustcapture_core::orchestrator::{AppEvent, CaptureRequest, Orchestrator};
use rustcapture_core::output::FileSink;

fn main() -> ExitCode {
    platform_win::dpi::ensure_per_monitor_dpi_awareness();

    let options = match args::parse(std::env::args_os().skip(1).collect()) {
        Ok(o) => o,
        Err(e) => {
            eprintln!("error: {e}\n\n{}", args::USAGE);
            return ExitCode::from(2);
        }
    };

    let mut orch = Orchestrator::new(Box::new(GdiScreenSource::new()), Box::new(create_mode));
    let destination = match options.destination {
        args::Destination::Clipboard => {
            orch.add_sink(Box::new(ClipboardSink::new()))
                .expect("primer sink registrado");
            "clipboard"
        }
        args::Destination::File { dir, format } => {
            orch.add_sink(Box::new(FileSink::new(dir, format)))
                .expect("primer sink registrado");
            "file"
        }
    };

    let (tx, rx) = mpsc::channel();
    tx.send(AppEvent::CaptureRequested(CaptureRequest {
        mode: options.mode,
        destination,
    }))
    .expect("canal recién creado");
    drop(tx); // sin más productores: run termina al vaciar la cola

    let mut fallo = None;
    orch.run(rx, |_, result| {
        if let Err(e) = result {
            fallo = Some(e.to_string());
        }
    });

    match fallo {
        Some(e) => {
            eprintln!("error: {e}");
            ExitCode::FAILURE
        }
        None => {
            println!("captura entregada a {destination}");
            ExitCode::SUCCESS
        }
    }
}
```

- [ ] **Step 2: Compilar y correr los tests**

Run: `cargo fmt && cargo test -p cli`
Expected: PASS (los 8 tests de args siguen en verde; el binario compila).

- [ ] **Step 3: Verificación manual del binario real (caso de uso del plan)**

En PowerShell o Bash, desde la raíz del repo (`<SCRATCH>` = un directorio temporal de prueba):

```bash
# 1. Flags inválidas → exit 2 + uso en stderr
cargo run -q -p cli -- --sepia; echo "exit: $?"

# 2. Región → archivo PNG real
cargo run -q -p cli -- --region 0,0,50x50 --file --dir "<SCRATCH>/cli_test"
ls "<SCRATCH>/cli_test"   # debe listar captura_*.png

# 3. Fullscreen → portapapeles (default sin flags)
cargo run -q -p cli --
```

Expected: (1) exit 2 con uso; (2) exit 0, mensaje "captura entregada a file" y un `captura_*.png` de 50x50 en el directorio; (3) exit 0, "captura entregada a clipboard" (el portapapeles del humano queda con la captura de pantalla — avisar).

- [ ] **Step 4: Staging**

```bash
git add crates/cli/
```

---

### Task 3: Verificación final del slice y cierre

**Files:**
- Modify: `roadmap.md` (marcar ✅ el ítem de la CLI, solo tras verificar)

**Interfaces:**
- Consumes: todo lo anterior.
- Produces: slice verificado; propuesta de commit al humano.

- [ ] **Step 1: Verificación completa (skill `verification-before-completion`)**

```bash
cargo build --workspace
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
cargo fmt --check
```

Expected: build limpio; 53 core + 6 platform-win + 8 cli = 67 tests; clippy y formato limpios.

- [ ] **Step 2: Revisión de contrato**

- `cli` no contiene lógica de dominio: solo parsing y cableado (D1).
- La captura viaja por el bus (`AppEvent::CaptureRequested`), no por llamada directa (D7).
- `pico-args` sin dependencias transitivas nuevas en `Cargo.lock`.

- [ ] **Step 3: Actualizar roadmap**

En `roadmap.md` §2, cambiar:

```
- ⏳ CLI fina sobre el core (f.8, D1).
```

por:

```
- ✅ CLI fina sobre el core (f.8, D1).
```

- [ ] **Step 4: Proponer el commit al humano (NO ejecutar sin aprobación)**

Mensaje propuesto:

```
v0.1.6 — F1: CLI fina (rustcapture.exe)

Primer binario usable: flags → ModeRequest/Destino (pico-args, parsing
puro testeado) → evento CaptureRequested por el bus → GDI → sink real.
Defaults --fullscreen --clipboard; exit codes 0/1/2. Verificado de punta
a punta en máquina real (PNG en disco y portapapeles).
```
